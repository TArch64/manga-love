# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  email: String
}

type Success {
  success: Boolean!
}

type ResetPasswordStateType {
  isValid: Boolean!
}

type EmailVerificationStateType {
  isValid: Boolean!
  name: String!
}

type MangaPoster {
  id: ID!
  originalSrc: String!
  originalAspectRatio: Float!
}

type Manga {
  id: ID!
  createdAt: DateTime!
  originalName: String!
  uaName: String
  description: String!
  type: MangaType!
  releaseDate: DateTime
  finishDate: DateTime
  posters: [MangaPoster!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

enum MangaType {
  MANGA
  MANHUA
  MANHWA
}

type Query {
  currentUser: User!
  resetPasswordState(code: String!): ResetPasswordStateType!
  emailVerificationState(code: String!): EmailVerificationStateType!
  mangas(offset: Float!, limit: Float!): [Manga!]!
}

type Mutation {
  signIn(email: String!, password: String!): Success!
  googleSignIn(credential: String!): Success!
  signUp(username: String!, email: String!, password: String!): Success!
  askResetPassword(email: String!): Success!
  resetPassword(password: String!, code: String!): Success!
  verifyEmail(code: String!): Success!
}