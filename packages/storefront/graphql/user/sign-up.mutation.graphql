mutation($email: String!, $password: String!, $username: String!, $language: Language!) {
    signUp(email: $email, password: $password, username: $username, language: $language) {
        success
    }
}
